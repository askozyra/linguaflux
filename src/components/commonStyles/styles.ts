import { StyleService } from "@ui-kitten/components";

export const ScreenStyles = StyleService.create({
  Layout: {
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
});
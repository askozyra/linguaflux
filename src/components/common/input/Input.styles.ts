import { StyleService } from "@ui-kitten/components";

export const CustomInputStyles = StyleService.create({
  Layout:
  {
    borderWidth: 0,
    borderBottomWidth: 2,
    backgroundColor: '#fff1'
  },
});